COmponent Rerendering:

- IF parent rerender children also rerender
- Changes in probs
- if there is any change in state , componet will rerender 

State is an object to store Data within a component for a short period of time : state{} => useState
State is immutable , but we will get function 

for icons : https://react-icons.github.io/react-icons/